@using Mari.Client.Models
@using System.Security.Cryptography.X509Certificates
@inherits LayoutComponentBase

<MudThemeProvider />
<MudDialogProvider />
<MudSnackbarProvider />

<MudLayout>
    <MudAppBar Elevation="0" Bottom="true" Fixed="true">
        <AppBar DrawerButtonOnClick="DrawerButtonOnClick" />
    </MudAppBar>
    <MudDrawer @bind-Open="IsDrawerOpened">
        <NavMenu />
    </MudDrawer>

   
    @*<CascadingValue Value="ShowError">*@
        <MudMainContent Class="pt-0">
            @Body
        </MudMainContent>
      @*</CascadingValue >*@
</MudLayout>

@code
{
    private bool IsDrawerOpened { get; set; }

    private void DrawerButtonOnClick()
    {
        IsDrawerOpened = !IsDrawerOpened;
    }
    
    /*[Inject] private IDialogService DialogService { get; set; }
    
    public async void ShowError(string title, string message)
    {
        bool? result = await DialogService.ShowMessageBox(
            title, 
            message, 
            yesText:"Ok");
        
        StateHasChanged();
    }
    
    public async void ShowErrorTest()
    {
        bool? result = await DialogService.ShowMessageBox(
            "title", 
            "Код ошибки 404, идите нахуй, вы мне тут не нужны  Press F ", 
            yesText:"Ok");
        
        StateHasChanged();
    }*/
}